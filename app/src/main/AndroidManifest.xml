<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hendrixassassins">

    <uses-permission android:name="android.permission.VIBRATE" /> <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".uipages.EmailSenderActivity"></activity>
        <activity android:name=".CreateGameActivity" />
        <activity android:name=".NofiticationListView" />
        <activity android:name=".uipages.NotificationTemplateViewActivity" />
        <activity
            android:name=".uipages.HomeActivity"
            android:windowSoftInputMode="adjustNothing"
            android:label="@string/title_activity_home" />
        <activity android:name=".uipages.OpenScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".uipages.LoginActivity"
            android:label="@string/title_activity_login" />
        <activity android:name=".AgentProfileActivity" />
        <activity android:name=".UItestcompnents.ListViewTestActivity" />
        <activity android:name=".email.GmailTestActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>